<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.footballapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".view.activities.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.activities.TeamActivity" />
        <activity android:name=".view.activities.PlayerActivity" />
        <activity android:name=".view.activities.CreditsActivity" />
        <activity android:name=".view.activities.SettingsActivity" />
        <activity android:name=".view.activities.SearchTeamActivity" />
        <activity android:name=".view.activities.MatchActivity" />
        <activity android:name=".view.activities.ConnexionActivity" />
        <activity android:name=".view.activities.InscriptionActivity" />
        <activity android:name=".view.activities.MainActivity" />

        <service android:name=".services.FootLiteFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>